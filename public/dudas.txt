Angular:
- Estructura basica de proyectos y carpetas?
- Cuando un nuevo modulo? pages? layout? 
- Authenticacion? guards?
- Spotify - con api - back en nodejs o directo un services a la api? 
- css variables sass globales?

NodeJs 
- Estructura basica 

MySQL 
- Estructuras basicas de base de datos -- Triggers, procedures, views?









- ngOnInit what is?
- subscribe - asyncrhonous?
- private log ??
- Angular Material
- Why messages public - private 
- Que esta en el documento y que esta en la base de datos
- Handling Errors 
    - operation?
    - <T>
    - of() result as T 
- tap ? coje lo bueno? y el "_"
- cuando la funcion en subscribe es una arrow vacia, es cuando hay respuesta? put request example
- why httpOptions and not headers directo?
- selector # en html elementos, 
- trim() devuelve un objeto en base a un string?
- delete ---> el filter con h !== hero ??? que funcion cumple
- search 
    - tap --> x.lenght?
    - why AsyncPipe? benefit?
    Since *ngFor can't do anything with an Observable, use the pipe 
    character (|) followed by async. This identifies Angular's AsyncPipe 
    and subscribes to an Observable automatically so you won't have to do so
    in the component class.
    - SwitchMap?
    - heroSearchComponent
        - Put a trim in the subject?
        - Remember that the component class does not subscribe to the heroes$ observable. 
        That's the job of the AsyncPipe in the template.


#Ejercicio

Hacer 3 funciones en Javascript
listEmployees[] name, age, salary - 18-40años
10 employees
1. 
    1 parametro de entrada que es la listaAlumno
    devolver una lista que tenga todos los que tienen menos de 30 y ganan
    mas de 1500€

2.
    Funcion con dos parametros, lista y nombre
    devuelve lista con todos los que tienen ese name

3. 
    Funcion con 2 params, lista e increase % de salario.
    Retorna la lista con todos los salarios incrementados
